import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      subtitle="A placeholder operations board for field and office follow-ups."
      description="Phase 2 will attach assignees, due dates, priorities, and status updates to real task data."
      highlights={["Task queue shell", "Priority grouping", "Upcoming due-date panel"]}
    />
  );
}
