import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      title="Projects"
      subtitle="A scaffolded list view for active and planned jobs."
      description="Phase 2 will add project records, status filters, budget totals, and project detail flows."
      highlights={["Project table shell", "Status filter area", "Primary call-to-action slot"]}
    />
  );
}
